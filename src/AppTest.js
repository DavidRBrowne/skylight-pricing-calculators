import React from 'react';

const AppTest = () => {
  return (
    <div style={{ 
      backgroundColor: 'red', 
      color: 'white', 
      padding: '50px', 
      fontSize: '32px',
      minHeight: '100vh',
      textAlign: 'center'
    }}>
      <h1>🔥 TEST FILE LOADED! 🔥</h1>
      <p>If you can see this RED background with white text, the new file system is working!</p>
      <p>This confirms that new files are being loaded correctly.</p>
    </div>
  );
};

export default AppTest; 